# 开发环境配置（Windows 本地端到端测试使用）
splitter:
  # Seq 入口目录：扫描该目录下的所有文件夹作为订单
  inbox-dir: D:/youkang/splitter/Seq
  # 结果目录：拆分后的 Bam/Var/Sequence/QC 输出位置
  result-dir: D:/youkang/splitter/result
  # 归档目录：处理完成后的原始订单归档
  archive-dir: D:/youkang/splitter/archive
  scan-cron: "0/30 * * * * ?"
  ready-check-interval-seconds: 5
  delete-after-archive: false
  retry-times: 3
  retry-interval-ms: 1000

logging:
  file:
    name: D:/youkang/splitter/logs/splitter.log
  level:
    com.youkang.splitter: DEBUG
